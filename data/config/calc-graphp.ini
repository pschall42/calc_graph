; calc-graph - Basic template config file

; Loads regular config data
@calc-graph.ini@

; Overrides some values for profile
[Display]
Title           = calc-graph (Profile)
ShowFPS         = true
